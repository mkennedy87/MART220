class MyBuilding {
    constructor(x, y, w, h) {
        this.x = x;
        this.y = y;
        this.w = w;
        this.h = h;
    }

    getX() {
        return this.x;
    }

    setX(x) {
        this.x = x;
    }

    loadImage()
}